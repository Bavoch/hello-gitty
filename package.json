{
  "name": "hello-gitty",
  "version": "0.1.0",
  "description": "轻量桌面 Git 管理工具,为单人 AI 开发者定制",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --no-bundle"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
