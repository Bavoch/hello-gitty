{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "主窗口默认能力",
  "windows": ["main"],
  "platforms": ["macOS", "windows"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-always-on-top",
    "dialog:default"
  ]
}
