# 2026-08-11

## Hello Gitty 桌面 Git 管理工具(从零搭建)

**定位**:轻量桌面 Git 工具,为单人 AI 开发者定制。类 VS Code Git 面板但极简:全部暂存/取消暂存/提交/推送/拉取,提交信息与冲突解决全 AI。

**技术栈**:Tauri 2 + Rust 后端(git CLI 封装)+ 原生 HTML/CSS/JS 前端(无打包器,`withGlobalTauri: true` 用 `window.__TAURI__` 全局调用)。reqwest(rustls)走 OpenAI 兼容接口。

**关键文件**:
- `src-tauri/src/git.rs` — porcelain v2 解析 + 全部 git 操作(`git add -A`/`reset`/`commit -F -`/`push -u` 兜底/`pull`)
- `src-tauri/src/ai.rs` — 提交信息生成(Conventional Commits + 仓库历史风格参考)、冲突解决(读冲突标记文件 → AI 合并 → 写回 + git add,>80KB 拒绝)
- `src-tauri/src/config.rs` — 设置存 app_config_dir/settings.json(原子写)
- `src/` — 前端:文件分组(冲突/暂存/更改)、提交对话框(AI 生成可改)、冲突进度条(事件推送)、设置弹窗

**踩坑记录(重要,下次直接抄)**:
1. **crates.io 下载超时**:本项目网络到 static.crates.io 极慢(30s 传 0 字节),方案是项目级 `.cargo/config.toml` 配 rsproxy 镜像(sparse+https://rsproxy.cn/index/),1 分钟拉完全部依赖。
2. **porcelain v2 解析**:未修改状态字母是 `.`(点)不是空格;`.M` 表示仅工作区改动。一个文件 XY 为 `MM` 时应同时进 staged 和 unstaged 两个分组(VS Code 行为)。
3. **路径转义**:非 ASCII 路径被 C 风格引号包裹,八进制转义是 **UTF-8 原始字节**(带 = \345\270\246),必须按字节收集再整体 `from_utf8_lossy`,不能每个转义当独立码点。
4. **unquote 循环边界**:要处理到 `bytes.len()-1`(闭合引号)之前的最后一个字节,`i+1 < len-1` 会丢末尾字符。
5. Tauri 2 命令里用 `app.path()` 需要 `use tauri::Manager`。
6. 冲突测试要造真实冲突:同位置改同一行 + `git merge`,不能用无 remote 的 `git pull`(报的是"无远程"不是冲突)。

**状态**:单元测试 2/2 通过(生命周期 + 冲突检测)。构建产物:target/release/bundle/macos/Hello Gitty.app。

**后续追加**:
- 修复了 porcelain `.` 语义 bug(未修改是 `.` 非空格)、unquote 的 UTF-8 字节解码、循环边界丢字符三个解析问题,测试全绿。
- 文件夹选择改为 Rust 侧 `pick_folder` 命令(dialog 插件 `blocking_pick_folder`),前端只依赖 `core.invoke`——避免插件 JS API 全局注入的不确定性;新版 dialog 插件 `into_path()` 返回 Result。
- 包标识符改为 `com.hellogitty.desktop`(`.app` 结尾会与 macOS 扩展冲突)。
- **DMG**:tauri 内置 create-dmg fork 在 macOS 上报 "Not enough arguments",用 `hdiutil create -volname "Hello Gitty" -srcfolder <app> -ov -format UDZO out.dmg` 手动打包成功(4.6MB)。
- 冒烟测试:产物二进制含 pick_folder,启动 4s 进程存活无崩溃。
- 交付:`dist/Hello Gitty_0.1.0_aarch64.dmg`(4.6MB)+ `.app`(13MB)。
- 沉淀技能 `tauri-git-tool`(用户级 ~/.workbuddy/skills/)。
- 仓库已提交:ba14659 骨架 + 9d04ba5 核心功能。

**菜单栏托盘常驻(commit 7a9266f)**:
- tauri 开启 `tray-icon` feature + `tauri-plugin-single-instance`。
- `TrayIconBuilder::with_id("hello-gitty-tray")`:左键单击聚焦窗口(`show_menu_on_left_click(false)`),右键菜单(显示 Hello Gitty / 退出)。注意 `.on_menu_event` 的 event.id.as_ref() 匹配 menu item id。
- 关闭隐藏:`on_window_event` 里 CloseRequested 调 `api.prevent_close()` + `window.hide()`;真正退出走托盘菜单。
- 单实例:`tauri_plugin_single_instance::init` 回调里 show+set_focus 已有窗口。
- 坑:`get_webview_window`/`default_window_icon` 需要 `use tauri::Manager`。
- 托盘/单实例均无需额外 capabilities 权限。

**提示词预设与自定义(commit fc6144a)**:
- AiConfig 新增 `prompt_preset`(默认 "conventional",serde default)+ `custom_prompt`;旧配置前端 `{...DEFAULT_AI, ...settings.ai}` 合并补齐。
- `ai::presets()` 返回 4 个内置预设(conventional/concise/detailed/gitmoji),每条含 system + user_template,模板占位符 `{diff}` `{log}` `{lang}`;新命令 `ai_presets()` 供设置页展示。
- 生成逻辑:preset=="custom" 且模板非空 → 自定义(默认 system 兜底);否则按 id 匹配,未知 id 回退 conventional。
- 前端设置页:预设下拉 + 只读模板预览(pre 块,escapeHtml 防注入)+ 自定义 textarea 切换;自定义占位符提示写在 UI。
- 坑:format! 字符串里写占位符字面量要 `{{diff}}` 双括号转义;常量不能作为 format 命名参数捕获,用 `{}` 位置参数拼接;结构体 String 字段字面量要 `.into()`。
- 测试 5/5:新增占位符渲染、预设查找兜底、语言指令 3 个。

**多仓库管理(commit a73151d)**:
- Settings 新增 `repos: Vec<String>`(serde default);旧配置前端迁移:last_repo 不在列表时自动补入并保存。
- 后端命令:`repos_status_all`(遍历 repos 生成 RepoSummary 摘要:分支/超前落后/各计数/is_repo)、`repos_add/remove/set_current`(remove 时同步清 last_repo)。
- 前端:左侧 172px 侧栏 + 主区;每项状态点着色(绿干净/黄变更/红冲突/灰非仓库)+ 徽标(冲突红色高亮,数字拼 innerHTML 安全,分支用 escapeHtml);点击切换、悬停 ✕ 移除;操作后 refresh 末尾自动 loadRepos 同步徽标。
- 侧栏与工具栏共用 addRepo(pick_folder → repos_add → set_current → loadRepos + refresh)。
- 空状态两态:无任何仓库(引导添加)/ 当前仓库非 git 仓库(初始化按钮)。

**冲突聚焦拉取 + AI 自动合并(commit d294cb8)**:
- 冲突决策弹窗:拉取后若有冲突(前端 git_status 复查)→ 弹「拉取产生 N 个冲突」,按钮:AI 自动解决并合并(primary)/ 稍后手动处理;手动处理则冲突区块保留。
- `git::finish_merge`:用 `git rev-parse --git-dir` 定位 git 目录(兼容 worktree/相对路径),MERGE_HEAD 存在时读 MERGE_MSG(空则 "Merge")→ commit 完成合并提交;非合并状态返回 (false,"") 不报错。命令 `git_finish_merge` 返回 {merged, message}。
- AI 全部解决成功后自动调 finish_merge,UI 提示「已自动完成合并提交」。
- 冲突区块:`showPanel` 无冲突整节 `hidden`(不再常驻),文件级「AI 解决」保留(手动 merge/逐文件场景)。
- 测试 6/6:新增冲突解决后完成合并、无 merge 状态空操作。

**用户偏好**:极简 UI、按钮短文案(暂存/提交/推送)、注释只写为什么。
