# 2026-08-11

## Hello Gitty 桌面 Git 管理工具(从零搭建)

**定位**:轻量桌面 Git 工具,为单人 AI 开发者定制。类 VS Code Git 面板但极简:全部暂存/取消暂存/提交/推送/拉取,提交信息与冲突解决全 AI。

**技术栈**:Tauri 2 + Rust 后端(git CLI 封装)+ 原生 HTML/CSS/JS 前端(无打包器,`withGlobalTauri: true` 用 `window.__TAURI__` 全局调用)。reqwest(rustls)走 OpenAI 兼容接口。

**关键文件**:
- `src-tauri/src/git.rs` — porcelain v2 解析 + 全部 git 操作(`git add -A`/`reset`/`commit -F -`/`push -u` 兜底/`pull`)
- `src-tauri/src/ai.rs` — 提交信息生成(Conventional Commits + 仓库历史风格参考)、冲突解决(读冲突标记文件 → AI 合并 → 写回 + git add,>80KB 拒绝)
- `src-tauri/src/config.rs` — 设置存 app_config_dir/settings.json(原子写)
- `src/` — 前端:文件分组(冲突/暂存/更改)、提交对话框(AI 生成可改)、冲突进度条(事件推送)、设置弹窗

**踩坑记录(重要,下次直接抄)**:
1. **crates.io 下载超时**:本项目网络到 static.crates.io 极慢(30s 传 0 字节),方案是项目级 `.cargo/config.toml` 配 rsproxy 镜像(sparse+https://rsproxy.cn/index/),1 分钟拉完全部依赖。
2. **porcelain v2 解析**:未修改状态字母是 `.`(点)不是空格;`.M` 表示仅工作区改动。一个文件 XY 为 `MM` 时应同时进 staged 和 unstaged 两个分组(VS Code 行为)。
3. **路径转义**:非 ASCII 路径被 C 风格引号包裹,八进制转义是 **UTF-8 原始字节**(带 = \345\270\246),必须按字节收集再整体 `from_utf8_lossy`,不能每个转义当独立码点。
4. **unquote 循环边界**:要处理到 `bytes.len()-1`(闭合引号)之前的最后一个字节,`i+1 < len-1` 会丢末尾字符。
5. Tauri 2 命令里用 `app.path()` 需要 `use tauri::Manager`。
6. 冲突测试要造真实冲突:同位置改同一行 + `git merge`,不能用无 remote 的 `git pull`(报的是"无远程"不是冲突)。

**状态**:单元测试 2/2 通过(生命周期 + 冲突检测)。构建产物:target/release/bundle/macos/Hello Gitty.app。

**后续追加**:
- 修复了 porcelain `.` 语义 bug(未修改是 `.` 非空格)、unquote 的 UTF-8 字节解码、循环边界丢字符三个解析问题,测试全绿。
- 文件夹选择改为 Rust 侧 `pick_folder` 命令(dialog 插件 `blocking_pick_folder`),前端只依赖 `core.invoke`——避免插件 JS API 全局注入的不确定性;新版 dialog 插件 `into_path()` 返回 Result。
- 包标识符改为 `com.hellogitty.desktop`(`.app` 结尾会与 macOS 扩展冲突)。
- **DMG**:tauri 内置 create-dmg fork 在 macOS 上报 "Not enough arguments",用 `hdiutil create -volname "Hello Gitty" -srcfolder <app> -ov -format UDZO out.dmg` 手动打包成功(4.6MB)。
- 冒烟测试:产物二进制含 pick_folder,启动 4s 进程存活无崩溃。
- 交付:`dist/Hello Gitty_0.1.0_aarch64.dmg`(4.6MB)+ `.app`(13MB)。
- 沉淀技能 `tauri-git-tool`(用户级 ~/.workbuddy/skills/)。
- 仓库已提交:ba14659 骨架 + 9d04ba5 核心功能。

**菜单栏托盘常驻(commit 7a9266f)**:
- tauri 开启 `tray-icon` feature + `tauri-plugin-single-instance`。
- `TrayIconBuilder::with_id("hello-gitty-tray")`:左键单击聚焦窗口(`show_menu_on_left_click(false)`),右键菜单(显示 Hello Gitty / 退出)。注意 `.on_menu_event` 的 event.id.as_ref() 匹配 menu item id。
- 关闭隐藏:`on_window_event` 里 CloseRequested 调 `api.prevent_close()` + `window.hide()`;真正退出走托盘菜单。
- 单实例:`tauri_plugin_single_instance::init` 回调里 show+set_focus 已有窗口。
- 坑:`get_webview_window`/`default_window_icon` 需要 `use tauri::Manager`。
- 托盘/单实例均无需额外 capabilities 权限。

**提示词预设与自定义(commit fc6144a)**:
- AiConfig 新增 `prompt_preset`(默认 "conventional",serde default)+ `custom_prompt`;旧配置前端 `{...DEFAULT_AI, ...settings.ai}` 合并补齐。
- `ai::presets()` 返回 4 个内置预设(conventional/concise/detailed/gitmoji),每条含 system + user_template,模板占位符 `{diff}` `{log}` `{lang}`;新命令 `ai_presets()` 供设置页展示。
- 生成逻辑:preset=="custom" 且模板非空 → 自定义(默认 system 兜底);否则按 id 匹配,未知 id 回退 conventional。
- 前端设置页:预设下拉 + 只读模板预览(pre 块,escapeHtml 防注入)+ 自定义 textarea 切换;自定义占位符提示写在 UI。
- 坑:format! 字符串里写占位符字面量要 `{{diff}}` 双括号转义;常量不能作为 format 命名参数捕获,用 `{}` 位置参数拼接;结构体 String 字段字面量要 `.into()`。
- 测试 5/5:新增占位符渲染、预设查找兜底、语言指令 3 个。

**多仓库管理(commit a73151d)**:
- Settings 新增 `repos: Vec<String>`(serde default);旧配置前端迁移:last_repo 不在列表时自动补入并保存。
- 后端命令:`repos_status_all`(遍历 repos 生成 RepoSummary 摘要:分支/超前落后/各计数/is_repo)、`repos_add/remove/set_current`(remove 时同步清 last_repo)。
- 前端:左侧 172px 侧栏 + 主区;每项状态点着色(绿干净/黄变更/红冲突/灰非仓库)+ 徽标(冲突红色高亮,数字拼 innerHTML 安全,分支用 escapeHtml);点击切换、悬停 ✕ 移除;操作后 refresh 末尾自动 loadRepos 同步徽标。
- 侧栏与工具栏共用 addRepo(pick_folder → repos_add → set_current → loadRepos + refresh)。
- 空状态两态:无任何仓库(引导添加)/ 当前仓库非 git 仓库(初始化按钮)。

**冲突聚焦拉取 + AI 自动合并(commit d294cb8)**:
- 冲突决策弹窗:拉取后若有冲突(前端 git_status 复查)→ 弹「拉取产生 N 个冲突」,按钮:AI 自动解决并合并(primary)/ 稍后手动处理;手动处理则冲突区块保留。
- `git::finish_merge`:用 `git rev-parse --git-dir` 定位 git 目录(兼容 worktree/相对路径),MERGE_HEAD 存在时读 MERGE_MSG(空则 "Merge")→ commit 完成合并提交;非合并状态返回 (false,"") 不报错。命令 `git_finish_merge` 返回 {merged, message}。
- AI 全部解决成功后自动调 finish_merge,UI 提示「已自动完成合并提交」。
- 冲突区块:`showPanel` 无冲突整节 `hidden`(不再常驻),文件级「AI 解决」保留(手动 merge/逐文件场景)。
- 测试 6/6:新增冲突解决后完成合并、无 merge 状态空操作。

**同步提交单行贯穿(commit c94ff7c)**:mergeRows 识别 synced 行后,渲染单格 `.h-cell { grid-column: 1 / -1 }` 横贯两列,不再左右重复;差异行仍独占单列 + 绿/紫高亮。

**更多菜单(commit 211b8a8)**:删除侧栏 repo-item 悬停 ✕ 按钮(.repo-remove 样式与 renderRepoList 的 rm 按钮一并清理);工具栏删 btn-refresh 图标,新增 .more-wrap > btn-more + .more-menu 下拉(刷新 / 移除项目 danger 红);document click 收起菜单,btn-more 点击 stopPropagation;setBusy 禁用列表 btn-refresh→btn-more;移除项目仅作用于列表(不动磁盘)。

**切换性能优化(commit 7529f5c)**:切换延迟主因是每次 refresh 都全量 repos_status_all(N 仓库 × git status)。优化:① refresh 用 `updateSidebarCurrent(st)` 就地更新当前项(零 IO),全量扫描只留「更多→刷新」;② git_history 改为 async,spawn_blocking 并行 head/upstream/log(远程 log 依赖 upstream 串行),不再内部跑 status 取 branch(前端传 st.branch);③ 前端 Promise.all 并行 status+history。切换 git 调用从 5+N 次降到 ~4 次且并行。spawn_blocking 返回 JoinHandle,先全部 spawn 再依次 await 即并行,无需 tokio join 宏。

**幽灵按钮(commit 1c5d4d9)**:.tool-btn 全面 ghost 化:透明底+1px 描边(--border)+inline-flex 图标文字 gap5;hover 浅背景;primary=紫描边紫字(hover #6366f11a);danger=红描边(悬停 #e5533b1f);按钮内嵌 13px stroke SVG 图标(提交✓/推送↑/拉取↓/更多⋯,stroke-width 2.2 round)。弹窗/空状态按钮同步生效,统一风格。

**主面板精简(commit f493035)**:移除右侧工具栏的 repo-chip(仓库名)与「添加仓库」按钮、分隔线,全部由左侧仓库列表承载;同步删除 main.js 中 repo-name 引用与 bindEvents 里 btn-open 绑定、styles.css 的 .repo-chip/.toolbar-divider。注意侧栏的 .repo-name 类与主面板的 #repo-name 同名不同物,保留侧栏。

**提交模式可配置(commit 0300731)**:AiConfig 新增 `commit_mode`(serde default "auto",测试断言默认值)。auto = AI 生成后立即 `git_commit`;confirm = 展示弹窗可修改后提交;AI 失败时两种模式统一回退手动弹窗(showCommitDialog),保证提交路径永远可用。设置页「提交模式」下拉置于语言字段上方。

**提交不再自动暂存(commit ba3d449)**:提交只针对已暂存内容(标准 git 语义);`diff_for_ai` 增加 `staged_only` 参数(测试:取消暂存 b.txt 后 staged diff 含 a 不含 b),AI 提交信息只基于暂存差异;`updateToolbar` canCommit = staged>0,onCommit 前置检查无暂存时提示「先点击全部暂存」。注意:自动暂存曾写在 README 与旧记忆里,均已更正。

**移除底部状态栏(commit 73ee1d4)**:删除 #statusbar(分支/超前落后/计数)及其 JS/CSS 引用;忙碌指示 #sb-busy 迁移到工具栏右侧(margin-left:auto 的 .busy-ind,spinner 配色改为 --text-dim 透明顶边,适配浅底工具栏)。分支信息由左侧仓库列表承载。

**暂存操作移入列表标题行(commit 1818fc5)**:新增 .section-head-row(按钮行 = flex:1 的 section-head + 右侧 mini-btn);「更改」标题行放 #btn-stage-all「全部暂存」、「暂存的更改」标题行放 #btn-unstage-all「取消全部」;顶部工具栏精简为 提交/推送/拉取/刷新。按钮 id 不变,JS 逻辑零改动;mini-btn 与 section-head 是兄弟节点,点击不会误触折叠。

**窗口置顶(commit 63aa81b)**:顶栏图钉按钮(btn-pin)切换置顶,active 高亮(accent 色);后端 `window_set_always_on_top`(get_webview_window("main").set_always_on_top);capabilities 需加 `core:window:allow-set-always-on-top` 权限。状态不持久化,失败回滚。

**主题色统一(commit 0119136)**:UI 主色从蓝色改为 logo 靛紫 --accent #6366f1(indigo-500)、hover #7a6df6、浅色高亮 #6366f11a;所有强调色均走 CSS 变量,无硬编码残留。logo 中绿色(git 分支)为品牌辅助色保留。

**历史列表与强制回退(commit 42a568c)**:右侧面板底部「历史」区块(最近 20 条,格式 %H\t%h\t%an\t%at\t%s 用 splitn(5,'\t') 解析,subject 可能含 tab 时也不炸);HEAD 项紫色高亮 + HEAD 标签,回退按钮禁用;`git reset --hard <hash>` 走二次确认弹窗(danger 红按钮),警示"不可恢复";时间前端相对化(relTime)。测试:log 倒序/短 hash 7 位/head 匹配/reset 后历史缩短。

**本地+远程双历史(commit 9d5eed4)**:git_history 扩展返回 {head, branch, commits, remote:{name, commits}};`git::upstream` 用 `rev-parse --abbrev-ref --symbolic-full-name @{u}` 取上游(无则 None),`log_ref(repo, ref, n)` 按分支取历史。前端历史区块分两组:「本地 · 分支」(HEAD 高亮)+ 远程组(紫色圆点标题 #8b5cf6);无上游显示「无远程分支」;远程条目也可强制回退。测试:裸仓库 init --bare 需先 create_dir_all(否则 git init 报 cannot change to),push -u 后 upstream=origin/master、log_ref 可取。

**历史并排对比(commit d762c35)**:两列 grid(1fr 1fr)并排,左侧本地右侧远程;`mergeRows` 拉链合并:hash 相同 → 同行(i++/j++),否则按时间戳大的先放(单侧行);差异高亮:仅本地=绿底 #89d1851a、仅远程=紫底 #8b5cf61a;单元格 = 短 hash + HEAD 标签 + 截断 msg + 回退按钮,完整信息(时间/作者)放 title。注意并排后列宽小,字号降为 10-11.5px、回退按钮文案改「回退」。

**回退按钮悬停显示(commit 174e5ee)**:visibility:hidden 默认隐藏(不占位不跳动),.h-cell:hover 显示;HEAD 项始终隐藏。

**自动创建远程仓库(commit f3c4b69)**:Settings 加 `github_token`;git_push 命令改为 (app, repo):先 `git remote get-url origin` 检测,有 origin 走普通 push;无 origin 且 token 非空 → `auto_create_and_push`(POST api.github.com/user/repos {name:目录名, private:true, auto_init:false} → clone_url → remote remove/add origin → git::push -u);无 token 返回明确提示。reqwest 已存在无需新依赖;GitHub API 需 User-Agent 头。注意:创建后推送认证走系统凭据(Keychain),失败会提示。

**用户偏好**:极简 UI、按钮短文案(暂存/提交/推送)、注释只写为什么。

**分支 master → main 迁移(2026-08-11 晚)**:hello-gitty 默认分支由 git init 默认的 master 迁移为 main:`git branch -m master main` + `git push -u origin main` + gh 改远程默认分支(`gh repo edit <repo> --default-branch main`,因 GitHub 拒绝删除当前默认分支)+ `git push origin --delete master` + `git remote prune origin`。**全局配置 `init.defaultBranch=main`(用户要求,以后新仓库默认 main)**。注意:推送 GitHub 曾报 LibreSSL SSL_ERROR_SYSCALL,重试即成功(暂时性网络)。gh CLI 本机可用且已登录(自动创建远程、改默认分支都走它)。

**幽灵按钮(commit 1c5d4d9)**:.tool-btn 全面 ghost 化:透明底+1px 描边(--border)+inline-flex 图标文字 gap5;hover 浅背景;primary=紫描边紫字(hover #6366f11a);danger=红描边(悬停 #e5533b1f);按钮内嵌 13px stroke SVG 图标(提交✓/推送↑/拉取↓/更多⋯,stroke-width 2.2 round)。弹窗/空状态按钮同步生效,统一风格。

**按钮去色(commit 9f1049e)**(用户确认范围=仅按钮):操作按钮全部中性化——primary 用 font-weight 600 加粗区分(不用颜色),danger(移除/强制回退)与普通按钮同色,.rollback 历史回退按钮 var(--text)、hover 用 var(--hover);icon-btn.active 置顶激活由紫变常驻浅背景(不靠颜色);信息颜色保留:状态点/徽标/差异高亮/HEAD 紫/警告文字。**用户偏好:操作不用颜色区分,信息展示可用颜色**。

**移除顶栏(commit f9a6573)**:删除 #topbar(品牌 logo + 应用名 + 右侧按钮)及其样式(.brand/.logo/.topbar-right);置顶 btn-pin 与设置 btn-settings 移入侧栏底部 .sidebar-footer(margin-top:auto 贴底,分隔线上方,图标按钮同款)。窗口顶部现为系统标题栏。JS 无改动(元素 id 不变)。

**自动创建远程:多凭据来源(commit 7ca8d1b)**(用户问"能否不填 Token"):git_push 无 origin 时按优先级探测:① 设置页 github_token → GitHub API;② `gh_authed()`(gh auth status)→ `auto_create_via_gh`:gh repo create <name> --private --source <repo> --remote origin --push 一键完成;③ `system_github_token()`:`git credential fill`(protocol=https,host=github.com)读 Keychain,仅接受 ghp_/github_pat_/gho_ 前缀的 token 形态凭据;④ 全无 → 提示三种配置方式。run_gh 依次尝试 gh、/opt/homebrew/bin/gh、/usr/local/bin/gh。

**浏览器授权替代设置页 Token(commit e6d05db)**(用户要求:去掉设置页填 Token,无凭据时打开浏览器认证页):Settings.github_token 字段删除(旧配置多余字段 serde 自动忽略,无需迁移);git_push 无 origin:gh → Keychain → 全无返回错误前缀 `[NEED_AUTH]`(前端识别后 invoke open_auth_page 用 `open` 命令打开 https://github.com/settings/tokens/new?scopes=repo&description=Hello+Gitty,弹一次性粘贴框 dlg-token);`git_push_with_token` 先 `credential_approve`(`git credential approve` 写 stdin protocol/https/host/username/password 入 osxkeychain)再 API 创建+push,此后推送自动走 Keychain 策略零配置。注意 git credential fill/approve 的 stdin 必须写 `\n\n` 结尾才返回。

**新仓库推送后远程历史不显示(commit fc8d784)**:复现确认 push -u 后 upstream/remote log/remote-tracking 均正常,定位根因为 gh repo create --push 可能不设 upstream → `@{u}` 失败 → 远程列空。修复:① git_history 远程来源 `upstream().or_else(first_remote_branch)`(`for-each-ref --format=%(refname:short) refs/remotes`,排除 origin/HEAD);② auto_create_via_gh 后若 upstream 缺失补 `git push -u origin <branch>`。测试:push 后 first_remote_branch=origin/master。

**历史窗口错位(commit 5f78ac3)**:现象"最早的提交只在远程显示"。实测 hello-gitty:本地 31 条、远程 25 条(本地领先 6 条未推)。根因:本地/远程各取最近 20 条,本地窗口把未推的 6 条也算进"最近",窗口起点不同 → 底部最早的提交只落在远程窗口。修复:本地取 20+ahead、远程取 20+behind(`git rev-list --count remote..HEAD` / `HEAD..remote`),两侧下界对齐(clamp 20..500)。新测试:push 后 ahead/behind=0,本地新增提交后 ahead=1。

**项目图标(commit 922a378)**:侧栏仓库项显示项目图标——`repo_icon_data_url` 扫描根目录常见图标名(logo/icon/app-icon/appicon/app/favicon + png/jpg/jpeg/svg/webp/gif/ico,忽略大小写,512KB 上限),读文件转 base64 data URL(依赖 base64 0.22 crate);RepoSummary 加 `icon` 字段。前端有图标显示 16px img(.repo-icon-wrap 相对定位),状态点缩小叠加右下角(.dot-small 7px,1.5px panel 边框);无图标保持原状态点。

**测试适配(重要,全局 init.defaultBranch=main 影响所有测试)**:用户配置了全局默认分支 main,git.rs 测试中所有硬编码 master 的必须改用 `git symbolic-ref --short HEAD` 取当前分支名;detects_conflicts 必须在 checkout -b feature **之前**记录主分支名(否则取到 feature 自己,提交落在 feature 上导致 merge 快进无冲突)。
